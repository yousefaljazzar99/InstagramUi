import 'package:flutter/material.dart';

class FaverateScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Center(
      child: Text('Faverate Page'),
    );
  }
}
